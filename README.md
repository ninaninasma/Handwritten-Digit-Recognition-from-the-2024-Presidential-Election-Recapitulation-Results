# Handwritten-Digit-Recognition-from-the-2024-Presidential-Election-Recapitulation-Results
Collected a digit dataset from the SIREKAP website, followed by data preprocessing, feature extraction, and classification using Support Vector Machine (SVM) and K-Nearest Neighbor (KNN). The highest accuracy was achieved using HOG + Zoning feature extraction combined with SVM classification.
